let pass=sessionStorage.getItem('kiddy_admin')||'',products=[],reviews=[],orders=[];const $=s=>document.querySelector(s);async function api(u,o={}){o.headers={...(o.headers||{}),'x-admin-password':pass};return fetch(u,o).then(r=>r.json())}async function refresh(){products=await api('/api/products');reviews=await api('/api/reviews');orders=await api('/api/admin/orders');$('#sp').textContent=products.length;$('#so').textContent=orders.length;$('#sr').textContent=reviews.length;$('#pt').innerHTML=products.map(p=>`<div class=row><b>${p.name}</b> — ${p.category} — ৳${p.price} — Stock ${p.stock}</div>`).join('');$('#ot').innerHTML=orders.length?orders.map(o=>`<div class=row><b>${o.orderNo}</b> — ${o.customer.name} — ৳${o.total} — <select onchange="status('${o.orderNo}',this.value)"><option>${o.status}</option><option>Processing</option><option>Shipped</option><option>Delivered</option><option>Cancelled</option></select></div>`).join(''):'<p>No orders yet.</p>';$('#rv').innerHTML=reviews.map(r=>`<div class=row><b>${r.name}</b> — ${r.rating}★ — ${r.text} <button class=action onclick="delR(${r.id})">Delete</button></div>`).join('')}function tab(n){document.querySelectorAll('.tab').forEach(x=>x.style.display='none');$('#'+n).style.display='block';$('#title').textContent=n[0].toUpperCase()+n.slice(1);refresh()}document.querySelectorAll('[data-t]').forEach(b=>b.onclick=()=>tab(b.dataset.t));$('#loginBtn').onclick=async()=>{let p=$('#pass').value,r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:p})}).then(x=>x.json());if(r.ok){pass=p;sessionStorage.setItem('kiddy_admin',p);$('#login').style.display='none';$('#app').style.display='flex';refresh()}else $('#msg').textContent='Wrong password'};if(pass){$('#login').style.display='none';$('#app').style.display='flex';refresh()}$('#logout').onclick=()=>{sessionStorage.removeItem('kiddy_admin');location.reload()};async function status(no,s){await api('/api/admin/orders/'+no,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:s})});refresh()}async function delR(id){await api('/api/reviews/'+id,{method:'DELETE'});refresh()}
